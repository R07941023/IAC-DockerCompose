server {
    listen 443 ssl;
    server_name \
        vscode.mydormroom.com \
        vscode.mydormroom.dpdns.org \
        vscode-mydormroom.ddns.net;

    ssl_certificate /etc/nginx/conf.d/mydormroom.crt;
    ssl_certificate_key /etc/nginx/conf.d/mydormroom.key;

    client_max_body_size 0;
    proxy_buffering off;

    location / {
        proxy_pass http://vscode-server:8090;
        proxy_set_header Host $http_host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}

server {
    listen 443 ssl;
    server_name \
        vscode-dev.mydormroom.com \
        vscode-dev.mydormroom.dpdns.org \

    ssl_certificate /etc/nginx/conf.d/mydormroom.crt;
    ssl_certificate_key /etc/nginx/conf.d/mydormroom.key;

    client_max_body_size 0;
    proxy_buffering off;

    location / {
        proxy_pass http://vscode-server:3000;
        proxy_set_header Host $http_host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}

server {
    listen 443 ssl;
    server_name \
        vscode-docker.mydormroom.com \
        vscode-docker.mydormroom.dpdns.org \

    ssl_certificate /etc/nginx/conf.d/mydormroom.crt;
    ssl_certificate_key /etc/nginx/conf.d/mydormroom.key;

    client_max_body_size 0;
    proxy_buffering off;

    location / {
        proxy_pass http://host.docker.internal:30102;
        proxy_set_header Host $http_host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_connect_timeout 600s;
        proxy_send_timeout 600s;
        proxy_read_timeout 600s;
        send_timeout 600s;
    }
}